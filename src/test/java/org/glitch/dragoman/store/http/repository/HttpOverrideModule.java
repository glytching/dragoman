package org.glitch.dragoman.store.http.repository;

import com.google.inject.AbstractModule;
//import org.glitch.dragoman.repository.router.HttpRouter;
//import org.mockito.Mockito;

public class HttpOverrideModule extends AbstractModule {

    @Override
    protected void configure() {
        //        bind(HttpRouter.class).toInstance(Mockito.mock(HttpRouter.class));
    }
}
